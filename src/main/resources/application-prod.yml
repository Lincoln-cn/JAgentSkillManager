spring:
  config:
    activate:
      on-profile: prod

logging:
  level:
    org.unreal: INFO
    org.springframework: WARN

# Production overrides: disable hot reload
agent:
  skill:
    hot-reload-enabled: false
    auto-load-skills: true
